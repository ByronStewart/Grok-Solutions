# ask the user for their name
name = input("What is your name? ")

# create the start of the message
message_start = "So you call yourself '"

# create the end of the message
message_end = "' huh?"

# join the message start, name and message end to create the message
message = message_start + name + message_end

# print the message
print(message)
