# ask the user for their name
name = input("What is your name? ")

# create the start of the message
message_start = "Hello, "

# join the message start and the name to create the message
message = message_start + name

# print the message
print(message)
